# DuckDNS configuration for the free demo hostname.
# Copy to /etc/creative-intelligence/duckdns.env and chmod 600.
# NEVER commit a real token: this template stays empty on purpose.
DUCKDNS_SUBDOMAIN=creative-intelligence
DUCKDNS_TOKEN=
